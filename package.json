{
  "name": "solar-system",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --parallel --filter \"./**\" build"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "typescript": "^5.3.2"
  }
}
